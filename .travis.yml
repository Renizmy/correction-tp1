language: java
dist: xenial

  
addons:
  sonarcloud:
    organization: "renizmy"
    token: "$SONARCLOUD_TOKEN"
    
jobs:
  include:
    - stage: deploy
      script: expect scripts/deploy.sh
  
  
branches:
  only:
    - develop    
     
cache:
  directories:
   - $HOME/.m2
    
services:
  - docker

